Fournir le fichier `client-ci-cd.yml` rangé dans `.github/workflows`.

Bonus - Pour les curieux, il y a également un `api-cd-bonus.yml` pour une pipeline permettant de :
- générer une image Docker de l'API
- la tag et la push sur le DockerHub
