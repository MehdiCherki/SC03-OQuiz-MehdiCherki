# SC04E04 - Microservice

## Objectifs de la journée

- Correction
- Microservice avec proxy de requête

## Correction

### Exercice 1 : Sauvegarder les informations du fichier en BDD

#### Prisma / BDD

1. Initialiser prisma

```bash
npx prisma init
```

2. Le schéma de la BDD

```prisma
generator client {
  // `prisma-client` et pas `prisma-client-js` (genère le code typescript esm et pas javascript / cjs)
  provider = "prisma-client"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model File {
  id               Int    @id @default(autoincrement())
  size             Int
  newFilename      String @map("new_filename")
  originalFilename String @map("original_filename")
  mimetype         String

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
}
```

3. Créer une BDD local

```sql
CREATE USER files_service WITH CREATEDB PASSWORD 'files_service';
CREATE DATABASE files_service WITH OWNER files_service;
```

4. Générer le code prisma et créer la BDD

```bash
npx prisma migrate dev --name init
```

#### Code

```typescript
// files-service/models/index.ts
import { PrismaClient } from '../../prisma/generated/client.ts';

// On réexporte tous les modèles pour faciliter leur utilisatation dans le reste de l'application
export * from '../../prisma/generated/client.ts';

// On exporte une connexion à la base de données
export const prisma = new PrismaClient();
```

```typescript
// files-service/src/controllers/upload.controller.ts
export async function uploadFile(req: Request, res: Response) {
  const [fields, files] = await formParser.parse(req);

  // files est un objet avec des propriétés contenant des tableaux de fichiers uploadés
  // ex: { file: [{...}], image: [{...}] }

  // A manipuler, cela peut être contraignant car on doit savoir quelle propriété est utilisée pour l'upload
  // Moi je ne souhaite que récupérer les tableaux de fichiers uploadés
  // je souhaite passer de { file: [{...}], image: [{...}] } à un tableau de fichiers [{...}, {...}]

  // 1ère transformation : Object.entries(files)
  // { file: [{...}], image: [{...}] } => [['file', [{...}]], ['image', [{...}]]]
  const filesToSave = Object.entries(files)
    // 2ème transformation : on récupère les valeurs de chaque entrée
    // [['file', [{...}]], ['image', [{...}]]] => [[{...}], [{...}]]
    .map(([key, files]) => files || [])
    // 3ème transformation : on a un tableau de tableaux, on veut un tableau à plat
    // [[{...}], [{...}]] => [{...}, {...}]
    .flat();

  const filesCreated = await prisma.file.createManyAndReturn({
    data: filesToSave.map((file) => ({
      size: file.size,
      newFilename: file.newFilename,
      originalFilename: file.originalFilename || '',
      mimetype: file.mimetype || '',
    })),
  });

  res.json(filesCreated);
}
```

### Exercice 2 : Récupérer un fichier

```typescript
// files-service/src/controllers/upload.controller.ts
export async function getFile(req: Request, res: Response) {
  const { id } = idParamsSchema.parse(req.params);

  const file = await prisma.file.findUnique({
    where: { id },
  });

  if (!file) {
    // @TODO: gérer les erreurs de manière plus propre
    // On pourrait le remplacer par les erreurs custom fait dans la partie `api` de l'application
    // avec le handler d'erreurs global
    res.status(404).json({ error: 'File not found' });
    return;
  }

  // Vérifier si le fichier existe sur le disque
  const filePath = path.join(uploadDir, file.newFilename);
  if (!existsSync(filePath)) {
    res.status(404).json({ error: 'File not found on disk' });
    return;
  }

  res.sendFile(filePath);
}
```

### Exercice 3 : Modifier les images en live

```typescript
// files-service/src/validations/upload.ts
export const queryParamsGetFileSchema = z.object({
  resize: z
    .string()
    .regex(/^\d+(x\d+)?$/)
    .transform((value) => {
      const [width, height] = value.split('x').map(Number);
      return { width, height };
    })
    .optional(),
  quality: z.coerce.number().min(1).max(100).optional(),
  rotate: z.coerce.number().int().max(360).min(-360).optional(),
  blur: z.coerce.number().int().min(0).optional(),
  format: z.enum(['webp', 'jpg', 'png', 'jpeg']).optional(),
  negate: z
    .enum(['true', 'false'])
    .transform((v) => v === 'true')
    .optional(),
  tint: z
    .string()
    .regex(/^#[0-9A-Fa-f]{3,6}$/)
    .optional(),
  crop: z
    .string()
    .regex(/^\d+,\d+,\d+,\d+$/)
    .transform((v) => {
      const [left, top, width, height] = v.split(',').map(Number);
      return {
        left,
        top,
        width,
        height,
      };
    })
    .optional(),
});
```

```typescript
// files-service/src/controllers/upload.controller.ts
export async function getFile(req: Request, res: Response) {
  const { id } = idParamsSchema.parse(req.params);
  const queryParams = queryParamsGetFileSchema.parse(req.query);

  const file = await prisma.file.findUnique({
    where: { id },
  });

  if (!file) {
    // @TODO: gérer les erreurs de manière plus propre
    // On pourrait le remplacer par les erreurs custom fait dans la partie `api` de l'application
    // avec le handler d'erreurs global
    res.status(404).json({ error: 'File not found' });
    return;
  }

  // Vérifier si le fichier existe sur le disque
  const filePath = path.join(uploadDir, file.newFilename);
  if (!existsSync(filePath)) {
    res.status(404).json({ error: 'File not found on disk' });
    return;
  }

  // Si le fichier n'est pas une image
  // Ou si aucune transformation n'est demandée (queryParams vide)
  // On le renvoie tel quel
  if (!file.mimetype.startsWith('image/') || Object.keys(queryParams).length === 0) {
    res.sendFile(filePath);
    return;
  }

  // Sinon on va utiliser sharp pour modifier l'image
  const image = sharp(filePath);

  if (queryParams.resize) {
    image.resize(queryParams.resize.width, queryParams.resize.height);
  }

  if (queryParams.quality) {
    const format = file.mimetype.split('/')[1];
    image.toFormat(format as keyof sharp.FormatEnum, { quality: queryParams.quality });
  }

  if (queryParams.rotate) {
    image.rotate(queryParams.rotate);
  }

  if (queryParams.blur) {
    image.blur(queryParams.blur);
  }

  if (queryParams.format) {
    image.toFormat(queryParams.format);
  }

  if (queryParams.negate) {
    image.negate();
  }

  if (queryParams.tint) {
    image.tint(queryParams.tint);
  }

  if (queryParams.crop) {
    image.extract(queryParams.crop);
  }

  const buffer = await image.toBuffer();

  res.setHeader('Content-Type', queryParams.format ? `image/${queryParams.format}` : file.mimetype);
  res.setHeader('Content-Length', buffer.length.toString());

  res.send(buffer);
}
```

## Micro service

On reprend le schéma de l'application pour réexpliquer les micro services et on va insister sur le rôle du proxy.

Puis on va mettre en place le module [http-proxy-middleware](https://www.npmjs.com/package/http-proxy-middleware) dans le service `api`.

```bash
npm install http-proxy-middleware
```

```typescript
// api/src/routers/files.router.ts
import { Router } from 'express';
// import { checkRoles } from "../middlewares/access-control.middleware.ts";
import { createProxyMiddleware } from 'http-proxy-middleware';

export const router = Router();

const fileServiceProxyMiddleware = createProxyMiddleware({
  target: 'http://localhost:3015/api/files',
});

router.use(
  '/files',
  //checkRoles(['admin', 'author']),
  fileServiceProxyMiddleware
);
```

```typescript
// api/src/routers/index.router.ts
// ...
import { router as filesRouter } from './files.router.ts';
// ...

export const router = Router();

// ...
router.use('/files', filesRouter);
// ...
```

Puis démonstration

### Bonne pratique

Configuration en variable d'environnement du micro service `files-service`.

```typescript
// api/config.ts
export const config = {
  // ...
  filesServiceHost: process.env.FILES_SERVICE_HOST || 'http://localhost',
  filesServicePort: parseInt(process.env.FILES_SERVICE_PORT || '3015'),
};
```

```typescript
// api/src/routers/files.router.ts
const fileServiceProxyMiddleware = createProxyMiddleware({
  target: `${config.filesServiceHost}:${config.filesServicePort}/api/files`,
});
```

### Récap de la saison

- Logger
- Micro service
- Proxy
- Comment choisir le type de base de données ?
- MongoDB
  - Non structurée (pas de schéma de données)
  - Scalable
  - Performant en lecture / écriture (mais pas en modification)
- Docker
  - images => photo à un instant donné d'une configuration / application
  - volumes => stockage de données persistantes (un disque dur virtuel)
  - containers => VM qui est lancée à partir d'une image et potentiellement de volumes. Détruit lorsqu'on arrête le container.
